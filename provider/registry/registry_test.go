/*******************************************************************************
 * IBM Confidential
 * OCO Source Materials
 * IBM Cloud Container Service, 5737-D43
 * (C) Copyright IBM Corp. 2018 All Rights Reserved.
 * The source code for this program is not  published or otherwise divested of
 * its trade secrets, irrespective of what has been deposited with
 * the U.S. Copyright Office.
 ******************************************************************************/

package registry_test

//. "github.com/onsi/ginkgo"
//. "github.com/onsi/gomega"
//"github.ibm.com/arahamad/ibm-volume-lib/provider/registry"
//"github.ibm.com/arahamad/ibm-volume-lib/provider/registry/fakes"

/*
var _ = Describe("Registry", func() {
	var (
		fakeProvider     *fakes.Provider
		providerRegistry *registry.ProviderRegistry
	)

	BeforeEach(func() {
		fakeProvider = &fakes.Provider{}
		providerRegistry = &registry.ProviderRegistry{}
	})

	It("registers and retrieves providers", func() {
		providerRegistry.Register("test-provider", fakeProvider)

		provider, err := providerRegistry.Get("test-provider")
		Expect(err).NotTo(HaveOccurred())
		Expect(provider).To(Equal(fakeProvider))
	})

	Context("when getting a provider has not been registered", func() {
		It("returns an error", func() {
			provider, err := providerRegistry.Get("test-provider")
			Expect(err).To(HaveOccurred())
			Expect(provider).To(BeNil())
		})
	})
})*/
